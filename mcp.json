{
  "mcpServers": {
    "x402-scraper": {
      "command": "node",
      "args": ["index.js"],
      "env": {
        "PAYMENT_NETWORK": "base-mainnet",
        "SETTLEMENT_TOKEN": "USDC"
      },
      "description": "HTTP 402 paywalled web scraper tool for autonomous AI agents."
    }
  }
}
